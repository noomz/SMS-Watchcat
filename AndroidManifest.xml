<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.siriwat.sms.watchcat"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="7" android:maxSdkVersion="15" android:targetSdkVersion="13"/>

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:name="SMSWatchcatTabWidget"
            android:label="@string/app_name"
            android:theme="@style/LightThemeSwitcher" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name="SMSWatchcatNewFilter"
            android:label="@string/app_name" />
        
        <activity
            android:name="SMSWatchcatListFilter"
            android:label="@string/app_name" />
    </application>

</manifest>